version: "3.8"
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: recoveryops_admin
      POSTGRES_PASSWORD: local_dev_only
      POSTGRES_DB: recoveryops
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
